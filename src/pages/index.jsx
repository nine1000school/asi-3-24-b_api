import { useEffect, useState } from "react"

const IndexPage = () => {
  const [todos, setTodos] = useState([])

  useEffect(() => {
    ;(async () => {
      // axios
    })()
  }, [])

  return <table>...</table>
}

export default IndexPage
